<button
    type="button"
    id="pwa-install-button"
    class="{{ $class ?? 'hidden' }}"
    style="{{ $style ?? '' }}"
>
    <i class="bi bi-download"></i>
    <span>Install SIM-MMU</span>
</button>
